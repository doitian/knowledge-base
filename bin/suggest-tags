#!/usr/bin/env python3
import os
import sys
from pathlib import Path

# Delegate to the python script
script_path = Path(__file__).parent / "suggest-tags.py"
os.execv(sys.executable, [sys.executable, str(script_path)] + sys.argv[1:])
